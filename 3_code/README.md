This folder contains all the code used in the project. 

The training scripts are the ones that start with "training_" and end with the abbreviation of the model. Example: training_GRU.py → training GRU models.

